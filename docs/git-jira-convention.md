# SSAFY 자율 프로젝트 D109

# Git Commit Convention

## 커밋 메시지 구조

```text
[<scope>]<type>: <subject>
```

---

## Type (필수)

| Type | 설명 |
| --- | --- |
| `feat` | 새로운 기능 추가 |
| `fix` | 버그 수정 |
| `docs` | 문서 수정 (README, WIKI 등) |
| `refactor` | 코드 리팩토링 (기능 변경 없음) |
| `chore` | 빌드 설정, 패키지 매니저 설정 등 기타 자잘한 변경 |
| `revert` | 이전 커밋 되돌리기 |
| `remove` | 파일 삭제 |
| `design` | UI/UX 디자인 변경 (CSS 등) |

---

## Scope (필수)

- 간단하게 영역만 구분

---

## Subject (필수)

- 50자 이내로 작성
- 마침표(.) 사용하지 않음
- 한글로 간결하게 작성
- 명령형으로 작성 (예: "추가", "수정", "삭제")

---

## 커밋 메시지 예시

```text
[BE]fix: JWT 토큰 만료 시간 수정
```

---

## 브랜치 전략

---

### 브랜치 네이밍

| 브랜치 | 용도 | 네이밍 규칙 |
| --- | --- | --- |
| `main` | 배포 가능한 상태 유지 | - |
| `dev` | 개발 통합 브랜치 | - |
| `feat/*` | 기능 개발 | `feat/기능명` |
| `fix/*` | 버그 수정 | `fix/버그명` |
| `hotfix/*` | 긴급 수정 | `hotfix/이슈명` |

```text
[이슈타입]/[Jira이슈번호]-[간단한-설명]
```

### 브랜치 네이밍 예시

```text
feat/S14P31D109-42-social-login
bugfix/S14P31D109-105-token-expired-error
hotfix/S14P31D109-200-critical-login-failure
```

---

## MR(Merge Request) / PR(Pull Request) 규칙

### 리뷰는 필수

```text
리뷰전에 Merge금지
```

### 제목

```text
[Type] 작업 내용 요약
```

### 설정

```text
commit은 squash하여 요청하기
```

### 예시

```text
[S14P21D109-2][BE]Feat 회원가입 및 로그인 API 구현
[S14P21D109-3][BE]Fix 게임 점수 계산 오류 수정
[S14P21D109-4][BE]Refactor 공통 응답 DTO 구조 개선
```

### 본문 템플릿

```markdown
## 📌 작업 요약
<!-- 무엇을 했는지 -->

---

## 🔗 관련 이슈
- #

---

## ✅ 체크
- [ ] 정상 동작 확인
- [ ] 로컬 테스트 완료
```

---

# 📋 Jira Convention

## 1. 프로젝트 키 (Project Key)

- S14P21D109 으로 고정됨

---

## 2. 이슈 타입 (Issue Type)

| 이슈 타입 | 설명 | 예시 |
| --- | --- | --- |
| **Epic** | 큰 단위의 기능 묶음 (1~2주 이상 소요) | 회원 관리, 게임 플레이 |
| **Task** | 개발/기술 작업 단위 | API 설계, DB 스키마 작성 |
| **Bug** | 버그 수정 | 로그인 시 토큰 미발급 오류 |

---

## 3. 이슈 제목 (Summary) 컨벤션

### 카테고리 목록

| 카테고리 | 설명 |
| --- | --- |
| `[AOS]` | Android 앱 전반(모바일 + 워치 연동 포함) |
| `[FE]` | 프론트엔드(Web 등) |
| `[BE]` | 백엔드 |
| `[AI]` | AI / 머신러닝 |
| `[INFRA]` | 인프라 / 배포 |
| `[DB]` | 데이터베이스 |
| `[DESIGN]` | 디자인 |
| `[DOCS]` | 문서 작업 |

### Android 클라이언트 카테고리 사용 기준

- 기본값은 `[AOS]`를 사용한다.
- 폰 앱 수정하거나, 공통 계약/연동 로직을 포함하는 경우 `[AOS]`를 사용한다.

---

## 4. Epic 작성 컨벤션

### 네이밍 규칙

```text
[카테고리][도메인] 기능 대분류
```

### 예시

```text
[BE][회원] 회원 관리
[AOS][클라이밍] 녹화 및 워치 연동
[인프라] CI/CD 및 배포 환경 구축
```

### Epic 구조 예시

```text
Epic: [BE][회원] 회원 관리=
  ├── task: OAuth2 카카오 로그인 API 구현
  ├── task: JWT 토큰 발급 로직 구현
  ├── task: 프로필 수정 API 구현
  └── Task: 회원 테이블 스키마 설계
```

---

## 5. task 작성 컨벤션

### 형식

```text
앞에 카테고리나 도메인을 붙히지 않는다.
API단위로 짧게 짧게 작성한다
```

### 예시

```text
OAuth2 카카오 로그인 API 구현
JWT 토큰 발급 로직 구현
프로필 수정 API 구현
```

---

## 6. 워크플로우 (Workflow)

### 기본 상태 흐름

```text
TODO → IN PROGRESS → DONE
```

| 상태 | 설명 |
| --- | --- |
| **TODO** | 할 일 (백로그에서 스프린트로 이동됨) |
| **IN PROGRESS** | 작업 진행 중 |
| **DONE** | 완료 |

### 상태 전환 규칙

- `TODO → IN PROGRESS`: 담당자가 작업을 시작할 때
- `IN PROGRESS → DONE`: 리뷰 승인 및 머지 완료 시

---

## 7. 우선순위 (Priority)

| 우선순위 | 설명 | 사용 기준 |
| --- | --- | --- |
| 🔴 **Highest** | 즉시 처리 | 서비스 장애, 핵심 기능 블로커 |
| 🟠 **High** | 빠른 처리 필요 | 주요 기능, 다른 작업의 선행 조건 |
| 🟡 **Medium** | 일반 작업 | 스프린트 내 처리 대상 (기본값) |
| 🟢 **Low** | 여유 있을 때 처리 | 개선 사항, 리팩토링 |
| ⚪ **Lowest** | 나중에 처리 | Nice-to-have 기능 |

---

## 8. 스프린트 (Sprint) 컨벤션

### 스프린트 네이밍

```text
[프로젝트키] Sprint N (MM/DD ~ MM/DD)
```

### 예시

```text
S14P21D109 Sprint 1 (02/23 ~ 03/01)
S14P21D109 Sprint 2 (03/02 ~ 03/08)
```

### 스프린트 운영 규칙

- 스프린트 기간: **1주**
- 스프린트 시작 전: **스프린트 플래닝** 미팅 진행
- 스프린트 종료 시: **스프린트 리뷰** + **회고(Retrospective)** 진행
- 스프린트 중 이슈 추가는 최소화하고, 불가피한 경우 팀장 승인 후 추가

---

## 9. 스토리 포인트 (Story Point)

피보나치 수열 기반으로 산정합니다.

| 포인트 | 난이도 | 예상 소요 시간 | 예시 |
| --- | --- | --- | --- |
| **1** | 매우 쉬움 | ~1시간 | 오타 수정, 상수값 변경 |
| **2** | 쉬움 | ~2시간 | 단순 CRUD API 1개 |
| **3** | 보통 | ~3시간 | 일반적인 기능 구현 |
| **5** | 다소 복잡 | ~5시간 | 복잡한 비즈니스 로직 |
| **8** | 복잡 | 8~13시간 | 외부 API 연동 + 예외 처리 |

---

## 10. 라벨 (Label) 컨벤션

| 라벨 | 설명 |
| --- | --- |
| `frontend` | 프론트엔드 관련 |
| `backend` | 백엔드 관련 |
| `ai` | AI/ML 관련 |
| `infra` | 인프라/배포 관련 |
| `refactor` | 리팩토링 |
| `hotfix` | 긴급 수정 |
| `blocked` | 다른 이슈에 의해 블로킹됨 |

---

## 11. 컴포넌트 (Component)

프로젝트의 모듈/영역별로 컴포넌트를 설정합니다.

### 예시

```text
AOS       - Android App
Backend   - API Server
Backend   - Auth Module
AI        - Model Server
Infra     - CI/CD
Database  - Schema
```

---

## 12. 이슈 설명 (Description) 템플릿

### Bug 템플릿

```markdown
## 🐛 버그 설명
- 어떤 문제가 발생하는지 간단히 설명

## 🔄 재현 절차
1. Step 1
2. Step 2
3. Step 3

## ✅ 기대 결과
- 정상적으로 동작해야 하는 결과

## ❌ 실제 결과
- 현재 발생하는 오류 상황

## 📸 스크린샷
- (해당 시 첨부)

## 🖥️ 환경
- OS:
- 브라우저:
- 버전:
```

### Task 템플릿

```markdown
## 📌 작업 내용
- 수행해야 할 작업에 대한 설명

## ✅ 완료 조건
- [ ] 조건 1
- [ ] 조건 2

## 📎 참고 자료
- 관련 문서 링크
```

---

## 13. 보드 필터 (Quick Filters) 권장

| 필터명 | JQL |
| --- | --- |
| 내 이슈 | `assignee = currentUser()` |
| FE 이슈 | `labels = frontend` |
| BE 이슈 | `labels = backend` |
| 버그만 | `issuetype = Bug` |
| 블로킹 이슈 | `labels = blocked` |

---

## 14. 일일 운영 규칙

### Daily Standup (데일리 스크럼)

매일 아침 팀원들이 공유하는 내용:

1. **어제 한 일**: 완료한 Jira 이슈 공유
2. **오늘 할 일**: 진행할 Jira 이슈 공유
3. **블로커**: 진행을 방해하는 요소

### 이슈 관리 원칙

- 모든 작업은 반드시 Jira 이슈로 생성 후 진행
- 이슈 상태는 **실시간**으로 업데이트
- 하나의 이슈에는 **한 명의 담당자**만 배정
- 이슈가 커지면 Sub-task로 분리
- 완료된 이슈는 당일 내 **DONE** 처리
